ToDo:

* speed up nearestClassDistances(), see nearestClassDistances2()
* better memory handling for nearest.neighbour.distribution()
* add example with tif stack to readTIF, file in inst/extdata
* better way to handle URL in readTIF()
